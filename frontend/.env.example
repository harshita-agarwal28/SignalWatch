# Leave empty for local development - vite.config.ts proxies /api and
# /socket.io to http://localhost:4000, so the browser only ever talks to its
# own origin and no CORS setup is needed.
#
# Set this only when the backend is deployed at a different origin than the
# frontend (e.g. in production).
VITE_API_URL=
